//QUE: square a list of integers and display the count of even and odd values in a list in scala.
object SupriseTestGroupC extends App {
  def SquareAndCountEvenOdd(listOfInt: List[Int], sqrd: Any): Unit = {
    var countEven = 0
    var countOdd = 0
    var num
    listOfInt.map(num * num
    => sqrd
    )
    if sqrd % 2 == 0
    countEven += 1
    else countOdd += 1
  }

}
